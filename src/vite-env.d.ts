/// <reference types="vite/client" />

/**
 * Application version injected by Vite (see vite.config.ts), sourced from
 * package.json so the Settings footer cannot drift from the release version.
 */
declare const __APP_VERSION__: string;
