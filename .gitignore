# dependencies
node_modules

# build output
dist

# environment files — never commit real credentials
.env
.env.local
.env.*.local

# editor / OS
.DS_Store
*.log

# E2EE-2C provenance experiment — downloaded artifacts are not committed
/experiments/e2ee-2c-provenance/cache/
